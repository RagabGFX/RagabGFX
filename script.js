const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");io.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll(".reveal").forEach(e=>io.observe(e));
document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener("click",e=>{let t=document.querySelector(a.getAttribute("href"));if(t){e.preventDefault();t.scrollIntoView({behavior:"smooth"})}}));
const portrait=document.querySelector(".portrait");let ticking=false;function parallax(){if(!portrait)return;let y=Math.max(-35,Math.min(35,window.scrollY*.045));portrait.style.setProperty("--portrait-y",`${y}px`);ticking=false}window.addEventListener("scroll",()=>{if(!ticking){requestAnimationFrame(parallax);ticking=true}},{passive:true});parallax();
if(matchMedia("(pointer:fine)").matches){document.querySelectorAll(".talk,.logo,.explore").forEach(el=>{el.addEventListener("mousemove",e=>{let r=el.getBoundingClientRect();el.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.12}px,${(e.clientY-r.top-r.height/2)*.12}px)`});el.addEventListener("mouseleave",()=>el.style.transform="")})}